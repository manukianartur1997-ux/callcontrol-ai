{
  "name": "callcontrol-ai-demo-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny local backend for CallControl AI demo lead capture.",
  "scripts": {
    "build": "node build-pages.js",
    "start": "node server.js",
    "smoke": "node scripts/smoke-check.js",
    "check": "node --check server.js && node --input-type=module --check < functions/api/leads.js && node --check cloudflare/src/worker.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dejavu-fonts-ttf": "^2.37.3",
    "pdfkit": "^0.19.1"
  }
}
